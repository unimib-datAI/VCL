MONGO_URI=...
MONGO_INITDB_ROOT_USERNAME=...
MONGO_INITDB_ROOT_PASSWORD=...

GOOGLE_API_KEY=...
OPENAI_API_KEY=...

# Only needed when using -provider azure_openai (Azure OpenAI / Microsoft Foundry)
# Deployment name is passed via -model_name; api-version defaults in code unless overridden here.
AZURE_OPENAI_API_KEY=...
AZURE_OPENAI_ENDPOINT=...